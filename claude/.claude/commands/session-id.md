Print the current Claude Code session id.

Execute this bash command:

```bash
echo "$CLAUDE_CODE_SESSION_ID"
```

Do not ask for confirmation — just run it. Reply with the id only, no extra commentary.
